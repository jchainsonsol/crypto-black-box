const SOL='So11111111111111111111111111111111111111112';let current=null;const $=x=>document.getElementById(x),short=a=>a?`${a.slice(0,6)}…${a.slice(-6)}`:'—',pct=n=>`${Number(n||0).toFixed(n>=10?1:2)}%`;
async function scan(mint){status('Black Box is reading Solana mainnet…');$('scanButton').disabled=true;try{const response=await fetch(`/api/scan/${encodeURIComponent(mint)}`);const data=await response.json();if(!response.ok)throw Error(data.error||`Black Box API HTTP ${response.status}`);current=score(data);render(current);$('status').classList.add('hidden')}catch(e){status(`${e.message}. Start Black Box with node start.js and make sure your own Solana RPC is running on localhost:8899.`,true)}finally{$('scanButton').disabled=false}}
function score(r){let t1=r.holders[0]?.percentage||0,t5=r.holders.slice(0,5).reduce((a,h)=>a+h.percentage,0),t20=r.holders.reduce((a,h)=>a+h.percentage,0),risk=0,signals=[];if(r.info.mintAuthority){risk+=18;signals.push(sig('high','Mint authority active','Additional supply can be created.'))}else signals.push(sig('good','Mint authority revoked','No active mint authority reported.'));if(r.info.freezeAuthority){risk+=12;signals.push(sig('warn','Freeze authority active','Token accounts may be frozen.'))}else signals.push(sig('good','Freeze authority revoked','No active freeze authority reported.'));if(t1>20){risk+=18;signals.push(sig('high','Very high single-account concentration',`${pct(t1)} of supply is in the largest token account.`))}else if(t1>10){risk+=10;signals.push(sig('warn','Elevated single-account concentration',`${pct(t1)} of supply is in the largest token account.`))}else signals.push(sig('good','Largest-account concentration',`${pct(t1)} of supply is in the largest token account.`));if(t5>50){risk+=18;signals.push(sig('high','Top-five concentration extreme',`${pct(t5)} of supply is held by five token accounts.`))}else if(t5>30){risk+=10;signals.push(sig('warn','Top-five concentration elevated',`${pct(t5)} of supply is held by five token accounts.`))}else signals.push(sig('good','Top-five concentration',`${pct(t5)} across the five largest token accounts.`));let sc=100-Math.min(95,risk),label=sc<45?'HIGH RISK':sc<65?'ELEVATED':sc<80?'WATCH':'STRONG';return{...r,t1,t5,t20,score:sc,label,signals,bottom:signals.some(x=>x.level==='high')?'Black Box found high-risk control or concentration signals. Investigate before trusting the asset.':'The basic mint and concentration checks show no critical condition. Deeper wallet and liquidity intelligence is still required.'}}
function sig(level,title,text){return{level,title,text}}function render(r){$('report').classList.remove('hidden');$('mintDisplay').textContent=r.mint;$('scoreNumber').textContent=r.score;$('scoreLabel').textContent=r.label;$('scoreRing').style.setProperty('--score',r.score);$('bottomLine').textContent=r.bottom;$('supply').textContent=new Intl.NumberFormat('en-US',{notation:'compact'}).format(r.supply);$('decimals').textContent=`${r.info.decimals} decimals`;$('top1').textContent=pct(r.t1);$('top5').textContent=pct(r.t5);$('top20').textContent=pct(r.t20);$('mintAuthority').textContent=r.info.mintAuthority?short(r.info.mintAuthority):'REVOKED';$('freezeAuthority').textContent=r.info.freezeAuthority?short(r.info.freezeAuthority):'REVOKED';$('programType').textContent='SPL TOKEN';$('signalCount').textContent=`${r.signals.length} checks`;$('signals').innerHTML=r.signals.map(s=>`<div class="signal"><div>${s.level==='good'?'✓':s.level==='warn'?'!':'×'}</div><div><strong>${s.title}</strong><p>${s.text}</p></div><div class="severity sev-${s.level}">${s.level==='good'?'CLEAR':s.level==='warn'?'WATCH':'HIGH'}</div></div>`).join('');$('holderRows').innerHTML=r.holders.map(h=>`<tr><td>${h.rank}</td><td class="mono">${short(h.account)}</td><td class="mono">${short(h.owner)}</td><td>${new Intl.NumberFormat('en-US',{notation:'compact'}).format(h.amount)}</td><td>${pct(h.percentage)}</td></tr>`).join('');$('watchBtn').textContent=watched(r.mint)?'✓ WATCHING':'＋ WATCH TOKEN';scrollTo({top:$('report').offsetTop-90,behavior:'smooth'})}
function status(t,e=false){$('status').textContent=t;$('status').className='status'+(e?' error':'')}function list(){try{return JSON.parse(localStorage.getItem('cbb_watchlist')||'[]')}catch{return[]}}function watched(m){return list().some(x=>x.mint===m)}function watch(){if(!current)return;let a=list(),i=a.findIndex(x=>x.mint===current.mint);i>=0?a.splice(i,1):a.unshift({mint:current.mint,score:current.score,label:current.label});localStorage.setItem('cbb_watchlist',JSON.stringify(a));watchUI();$('watchBtn').textContent=watched(current.mint)?'✓ WATCHING':'＋ WATCH TOKEN'}function watchUI(){let a=list();$('watchlist').innerHTML=a.length?a.map(x=>`<div class="watch-item"><strong>${x.label} • ${x.score}/100</strong><div class="mono">${short(x.mint)}</div></div>`).join(''):'<div class="watch-empty">No watched tokens yet. Scan a mint and add it here.</div>'}$('scanForm').onsubmit=e=>{e.preventDefault();let m=$('mintInput').value.trim();m.length<32?status('Enter a valid Solana mint address.',true):scan(m)};$('exampleBtn').onclick=()=>{$('mintInput').value=SOL;scan(SOL)};$('demoBtn').onclick=()=>status('Black Box now uses our internal API.');$('watchBtn').onclick=watch;watchUI();